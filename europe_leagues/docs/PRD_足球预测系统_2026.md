---
title: 足球预测与数据系统 PRD（2026）
owner: europe_leagues
version: v1.2
last_updated: 2026-05-04
---

# 足球预测与数据系统 PRD（飞书文档版）

> 适用：飞书文档（Docs）  
> 项目目录：`/Users/bytedance/trae_projects/europe_leagues`
>
> 当前正式流程  
> 1. `prediction_system.py collect-data` 或赛程抓取定位 `match_id`  
> 2. `prediction_system.py predict-match / predict-schedule` 执行增强预测  
> 3. 结果写回 `europe_leagues/<league>/teams_2025-26.md`  
> 4. 赛后用 `prediction_system.py save-result` 或 `bulk_fetch_and_update.py` 回填  
> 5. 最后用 `prediction_system.py accuracy --refresh --json` 刷新胜负 / 比分 / 大小球统计  
> 可审计编排入口：`prediction_system.py harness-run --pipeline ... --json`  
> 关键检查项：`over_under.line`、`line_source`、`over_under.market.final`

---

## 目录
1. 背景与目标
2. 产品范围（In/Out）
3. 用户与使用场景
4. 系统总体架构
5. 端到端流程（确保可运行）
6. 模块拆解与文件管理（按目录/脚本/产物）
7. 数据规范（字段/来源/追溯）
8. 自动化 Skill 体系（可复用更新入口）
9. 运行保障与验收标准
10. 风险与待优化点（优先级）
11. 附录：常用命令清单

---

## 1. 背景与目标

> 当前说明：本文是产品/规划文档。若与执行细节冲突，以 `docs/standards/workflow.md`、`docs/standards/data_format.md` 和 `prediction_system.py` 当前 CLI 为准。

### 1.1 背景
本项目 `europe_leagues` 是面向五大联赛（英超/西甲/德甲/意甲/法甲）的比赛预测与分析系统，核心能力包括：
- 赛程维护（按轮次更新，避免误导比分）
- 球队/球员数据维护（中文名、号码、统计、高阶数据、热区等）
- 赔率落盘与未来赔率快照（用于相似盘路与爆冷分析）
- 预测生成与赛程备注写回
- 赛果回填与准确率统计（按联赛，覆盖胜负/比分/大小球）

### 1.2 目标（可量化）
- 能稳定生成未来 N 天五大联赛预测报告（默认 3 天，可扩展）。
- `players/*.json` 不再出现“全队空/球队输入退化导致预测内容一致”的问题。
- 赛程段落更新后：未完赛比分统一为 `-`（避免 0-0 误导）。
- 将“球员更新”“赛程更新”封装为可复用 Skill，后续一键统一更新。

---

## 2. 产品范围（In/Out）

### 2.1 In Scope（本 PRD 覆盖）
- 五大联赛赛程更新：写回 `teams_2025-26.md` 的赛程段落。
- 五大联赛球员数据更新：名单 → 中文名/号码 → 统计/热区 → 审计。
- 预测系统运行链路：输入数据读取优先级、赛程备注写回、运行时统计、结果统计。
- 文件管理规范：目录、产物、缓存、模板、脚本职责。

### 2.2 Out of Scope（暂不承诺）
- 全部联赛官方站点的完整伤病/首发阵容/训练报道等深度数据抓取（站点反爬强，需额外投入）。
- “评分/热图（触球热区）/传球成功率/防守对抗”等来自 WhoScored 的全量字段（抓取与授权成本高）。
  - 当前落盘：以 Understat/FPL/Sofascore/Wikidata 可稳定获取字段为准，并标注来源。

---

## 3. 用户与使用场景

### 3.1 目标用户
- 预测分析使用者：需要每天生成预测、关注爆冷风险、复盘准确率。
- 数据维护者：需要定期更新赛程、球员数据、赔率快照。

### 3.2 典型场景
- 场景 A：预测前，先统一更新赛程和球员数据，保证输入可信。
- 场景 B：转会窗后，一键刷新全量球员名单、号码、统计和热区。
- 场景 C：发现某联赛赛程/球员数据异常，快速重刷并输出审计报告定位缺失。

---

## 4. 系统总体架构（高层）

### 4.1 核心模块
| 模块 | 入口/文件 | 说明 |
|---|---|---|
| 统一入口 | `prediction_system.py` | 正式 CLI 入口，提供 `collect-data / predict-match / predict-schedule / save-result / accuracy / harness-run` |
| 增强预测 | `enhanced_prediction_workflow.py` | 主预测链路（动态调权、相似盘路、爆冷分析、赛程备注写回） |
| 旧流程兼容 | `optimized_prediction_workflow.py` | 优先调用增强预测器，失败回退旧逻辑 |
| 模型融合 | `ml_prediction_models.py` | 多模型融合输出（支持动态权重） |
| 结果管理 | `result_manager.py` | 赛果录入、准确率统计底层实现 |

### 4.2 数据分层（输入优先级）
- 赛程：`teams_2025-26.md`（赛程段落为权威落盘）
- 球员：`<league>/players/*.json`
- 赔率：
  1) `analysis/odds_snapshots/`（未来赛程即时快照）
  2) `analysis/odds/`（历史赔率落盘）
  3) 缺失时降级（报告会提示）

---

## 5. 端到端流程（确保可运行）

### 5.1 推荐执行顺序（每日/每周）
1. 更新赛程（五大联赛）
2. 更新球员数据（五大联赛）
3. `collect-data` 获取赛程、`match_id` 与上下文
4. `predict-match` / `predict-schedule` 生成预测并写回赛程备注列
5. 赛后录入结果并更新准确率

### 5.2 预测生成
- 入口：`python3 prediction_system.py predict-match ... --json`、`python3 prediction_system.py predict-schedule ... --json`
- 可审计入口：`python3 prediction_system.py harness-run --pipeline match_prediction ... --json`
- 输出：写回各联赛 `teams_2025-26.md` 赛程表备注列，运行时产物写入 `.okooo-scraper/runtime/`

### 5.3 缓存与可追溯
- `.prediction_cache/`：增强预测的 TTL 缓存（24h），可删（仅影响性能，不影响正确性）。

### 5.4 人工增强赛前分析流程（新增标准）
当用户要求对单场比赛做深度赛前预测时，统一采用下面这条链路，避免“只看模型”或“只看单一外部文章”：

1. 读取本地基础数据：
   - `teams_2025-26.md` 的赛程、积分、近期结果
   - `players/*.json` 的进球、助攻、xG、xA、关键传球与可出场信息
2. 读取爆冷知识库：
   - 默认接入 `爆冷案例库.json`
   - 有命中时作为风险修正项；无命中时明确写明“无直接案例支持”
3. 抓取临场市场数据：
   - 欧赔、亚盘、可稳定复核的凯利
   - 至少做 2 个外部来源交叉，不使用无法复核的单点截图结论
4. 做冲突校验：
   - 若本地模型、赔率方向、外部排名互相冲突，必须显式说明采用哪套口径
   - 不允许把来源冲突的数据直接混写成同一个结论
5. 输出统一结论：
   - 胜平负主结论
   - 2 至 3 个建议比分
   - 大小球倾向
   - 爆冷等级与是否需要防平/防冷
6. 结果落盘：
   - 赛前结论写入对应联赛 `teams_2025-26.md` 的赛程表备注列
   - 赛后再补实际比分，并刷新胜负/比分/大小球统计

---

## 6. 模块拆解与文件管理（强规范）

### 6.1 联赛目录规范（五大联赛一致）
| 路径 | 作用 |
|---|---|
| `<league>/teams_2025-26.md` | 球队信息 + 赛程信息 + 积分榜 + 数据来源 |
| `<league>/players/*.json` | 球队球员数据（名单/中文名/号码/统计/热区/来源） |
| `<league>/analysis/predictions/` | 历史模板/参考资料（非正式主流程输出） |
| `<league>/analysis/results/` | 历史模板/参考资料（非正式主流程输出） |
| `<league>/analysis/odds/` | 历史赔率落盘 |
| `<league>/analysis/odds_snapshots/` | 未来赔率快照落盘 |
| `.okooo-scraper/runtime/` | 运行时统计、日志、批量回填产物 |

### 6.2 当前正式产物
- 赛程写回：`<league>/teams_2025-26.md`
- 赔率快照：`.okooo-scraper/snapshots/<league>/*.json`
- 运行时统计：`.okooo-scraper/runtime/accuracy_stats.json`
- 缓存文件：`.prediction_cache/<md5>.json`（内部实现细节）

---

## 7. 数据规范（字段/来源/追溯）

### 7.1 players/*.json（核心字段规范）
| 字段 | 类型 | 说明 |
|---|---|---|
| `name` | string | 展示用中文名（优先），无法确认则保留英文 |
| `english_name` | string | 英文名（对齐 Understat/Sofascore/FPL） |
| `wikidata_id` | string | Wikidata QID（用于中文名/号码追溯） |
| `sofascore_player_id` | number | Sofascore ID（用于阵容/号码补齐追溯） |
| `shirt_number` | int/string | 单一号码（若唯一） |
| `shirt_numbers` | array | 多个号码备选（Wikidata 可能给出历史号码） |
| `stats.*` | number | 出场、分钟、进球、助攻、黄红牌等 |
| `advanced_stats.*` | number | Understat xG/xA 等高阶统计 |
| `heatmap.*` | object | 基于 Understat 射门坐标聚合的热区网格 |
| `transfer_status` | string | `current/injured/suspended/...`（英超可稳定补齐） |
| `injury_reason` | string | 伤病/缺阵原因（英超来自 FPL news） |
| `data_sources` | array | 标注来源，如 `understat.com/fantasy.premierleague.com/sofascore.com` |

### 7.2 teams_2025-26.md（赛程表格规范）
统一表头（所有联赛保持一致）：
| 日期 | 时间 | 主队 | 比分 | 客队 | 备注 |
|---|---|---|---|---|---|

强制规则：
- `status != 已结束` 时：`比分 = '-'`
- `备注` 用于记录机器可解析预测片段，如 `预测:主胜；比分:1-0/1-1；大小:小2.5(0.58)`

### 7.3 赛前预测备注规范（新增）
各联赛的赛前人工分析，需要统一写入各自 `teams_2025-26.md` 的赛程表备注列，建议格式如下：

- `预测:主胜`
- `比分:1-0/1-1`
- `大小:小2.5(0.58)`
- 赛后可追加 `✅/❌`

### 7.4 多源数据验证边界（新增）
为避免赛前报告出现伪精确或冲突结论，统一约束如下：

- 本地文件优先级：
  - 赛程与落盘状态以 `teams_2025-26.md` 为主
  - 球员产出与高阶数据以 `players/*.json` 为主
- 外部信息用途：
  - 赔率、盘口、凯利、伤病快讯、媒体前瞻用于补强
  - 外部排名若与本地积分表冲突，不直接覆盖本地表
- 冲突处理规则：
  - 若外部文章排名和本地积分冲突，优先比较“盘口方向 + 本地球员产出 + 本地近期赛果”
  - 若模型输出与市场明显相反，不直接采用模型结论，需人工复核后再落盘
  - 若凯利数据无法稳定复核，可以不报具体数值，但必须说明原因
- 明确不承诺项：
  - 不凭空生成首发名单
  - 不在缺少稳定来源时伪造凯利数值
  - 不把单一媒体文案当作最终结论

---

## 8. 自动化 Skill 体系（可复用更新入口）

### 8.1 球员更新 Skill
- Skill：`update-five-leagues-players`
- 文档：`.trae/skills/update-five-leagues-players/SKILL.md`
- 内容：球员名单 → 中文名/号码 → 统计/热区 → 审计

### 8.2 赛程更新 Skill
- Skill：`update-five-leagues-schedules`
- 文档：`.trae/skills/update-five-leagues-schedules/SKILL.md`
- 内容：500 轮次接口更新赛程段落，未完赛比分写 `-`

---

## 9. 运行保障与验收标准

### 9.1 运行保障（最低要求）
- 任意一天：能完成赛程更新、上下文采集、预测写回与统计刷新，无人工编辑依赖。
- `players/*.json` 不应出现全空导致“球队强度退化为默认值”的情况（若某队为空，审计应能定位）。

### 9.2 验收指标（建议）
- 赛程更新后：随机抽查 10 场未完赛，比分均为 `-`
- 球员数据：
  - 五大联赛每队至少 18 名球员（低于 18 的队触发“名单可能不全”提示）
  - 中文名命中率持续提升（允许保守跳过歧义短名）
  - 球衣号码缺失率可接受（来源不同可分层验收）

### 9.3 单场深度预测验收清单（新增）
一场纳入“正式赛前分析记录”的比赛，至少满足以下检查项：

- 已读取本地赛程和积分信息
- 已读取两队 `players/*.json`
- 已查询 `爆冷案例库.json`
- 已获取至少 1 组可复核欧赔/亚盘数据
- 若引用凯利，必须来自可稳定复核页面
- 若存在数据源冲突，已在结论里明确采用口径
- 已写入联赛 `teams_2025-26.md` 的赛程备注列

---

## 10. 风险与待优化点（优先级）

### P0（必须）
- 赛季参数可配置（Understat/FPL/官网文章 URL 可能每赛季变化），建议增加 `--season` 参数与统一配置文件。
- 跨源球员对齐：建议引入稳定 ID（Wikidata QID / Sofascore ID / FPL ID）做主键，降低误匹配风险。

### P1（重要）
- 中文名歧义处理：输出“待人工确认清单”，支持人工 CSV 覆盖（例如 `Manuel Ángel`、`Raúl`）。
- 数据来源可追溯增强：报告头部写入 players/odds/schedule 的更新时间戳与来源摘要。

### P2（增强体验）
- 评分/传球/防守等细项：若后续引入授权数据源（WhoScored/Opta等），可扩展字段，但必须标注来源与版本。

---

## 11. 附录：常用命令清单（表格版）

| 目的 | 命令 |
|---|---|
| 全量更新赛程（五大联赛） | `python3 update_schedules_from_500.py --league all` |
| 英超生成官方名单 CSV | `python3 generate_premier_league_players_csv.py` |
| 英超补中文名（常用译名） | `python3 fill_premier_league_player_name_cn.py` |
| 英超导入 CSV 到 players | `python3 import_players_from_csv.py --csv premier_league_players_2026.csv --league premier_league` |
| 英超补统计/伤病/热区 | `python3 enrich_premier_league_players_from_live_stats.py` |
| 四联赛 Understat 刷新 | `python3 sync_other_leagues_players_from_understat.py` |
| 四联赛补中文名/号码（Wikidata） | `python3 fill_other_leagues_player_name_cn_from_wikidata.py` |
| 四联赛补阵容/号码（Sofascore） | `python3 supplement_rosters_and_numbers_from_sofascore.py` |
| 完整性审计 | `python3 audit_players_completeness.py` |
